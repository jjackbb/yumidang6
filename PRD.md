# 유미당 (YouMeDang) 서비스 기능 및 제품 기획 명세서 (PRD)

| 항목 | 내용 |
| :--- | :--- |
| **서비스명** | 유미당 (YouMeDang) |
| **핵심 정의** | 취향 기반 1:1 라이프스타일 동행 매칭 플랫폼 |
| **핵심 슬로건** | "너(You)와 나(Me)가 함께하는(당:堂) 달콤하고 안전한 동행" |
| **문서 버전** | v1.2.0 (1:1 매칭 규격 및 당도 지표 최신 반영) |
| **최종 수정일** | 2026-09-11 |

---

## 1. 프로젝트 개요

### 1.1 배경 및 기획 의도
- 낯선 사람과의 동행이나 모임 수요는 급증하고 있으나, 기존 오픈채팅이나 커뮤니티는 신원 불확실성, 다인원 모임의 피로도, 안전에 대한 우려가 큽니다.
- 유미당은 **오직 1:1 동행 서비스만을 제공**하여 다인원 모임의 부담을 없애고, 둘만의 취향에 온전히 집중할 수 있는 만남을 연결합니다.
- 기계적인 매너온도 대신 유미당만의 달콤하고 친근한 신뢰 지수인 **'당도'** 시스템을 도입하여 참여자 간의 매너와 동행 만족도를 유쾌하고 직관적으로 관리합니다.

### 1.2 핵심 제품 원칙
1. **1:1 단일 매칭 원칙 (정원 2/2명 고정)**
   - 모든 동행 공고와 모집은 **호스트 1명 + 게스트 1명 = 총 2인**으로 엄격히 고정됩니다.
   - 모집 상태는 **`1/2명 (모집중)`**과 **`2/2명 (매칭완료)`** 두 가지로만 표기되며, 2인을 초과하는 다인원 모집은 일체 허용하지 않습니다.
2. **신뢰 지표: 당도 (Sugar Content)**
   - 기본 당도 50에서 시작하며, 긍정적인 상호 평가와 매칭 성공 경험을 통해 최고 100까지 축적됩니다.
   - 예: **`당도 99.2 🍯`** (기본 당도 50에서 49.2 상승)
3. **안전한 만남 보장**
   - 실명 마스킹(`조*미`, `강*훈`), 본인 인증회원 뱃지, 만남 장소 및 상세 일정의 확정자 전용 공개를 통해 프라이버시와 안전을 최우선으로 보호합니다.

---

## 2. 사용자 기능 정의서

### 2.1 홈 화면 (Home)
- **브랜드 헤더**: 유미당 공식 마스코트 캐릭터 로고(`logo.jpg`) 및 미확인 알림 뱃지
- **시즌 이벤트 배너**: 서울세계불꽃축제 등 대형 시즌 이벤트 큐레이션 및 1:1 동행 찾기 연계
- **매칭 확정 카드**: 현재 확정된 1:1 약속(D-1 카운트다운, 장소, 파트너 정보) 요약 및 대시보드 바로가기
- **12개 카테고리 그리드**:
  - `전시` (Palette), `축제` (PartyPopper), `식사` (Utensils), `운동` (Dumbbell), `여행` (Luggage), `클래스` (GraduationCap), `산책` (Footprints), `스터디` (BookOpen), `공연` (Ticket), `쇼핑` (ShoppingBag), `번개` (Zap), `전체보기` (LayoutGrid)

### 2.2 둘러보기 (Explore)
- 현재 동네(예: 강남구 대치동) 반경 내 등록된 1:1 동행 공고 리스트
- 지역 필터 칩 및 관심사(브런치, 전시, 산책 등) 키워드 검색
- 공고 카드 상태 표기: **`1/2명 (모집중)`** / **`2/2명 (마감)`**

### 2.3 1:1 동행 모집 등록 (Create Meetup)
- 하단 플로팅 액션 버튼(`+`)을 통해 진입
- **모집 형태 고정**: `1대1 맞춤 동행 (나 + 동행 파트너 1명, 2인 정원 고정)` UI 노출
- 카테고리 선택, 모집 제목, 날짜, 시간, 만날 장소, 해시태그 입력 후 등록 즉시 피드 반영 및 알림 생성

### 2.4 카테고리 & 이벤트 상세 모달
- **`CategoryDetailModal`**: 선택한 카테고리의 1:1 공고 리스트 노출, `1/2명` 및 `2/2명` 정원 표시, 즉시 `동행 참여하기` 토글
- **`EventDetailModal`**: 이벤트 공식 정보 소개 및 이벤트 전용 1:1 동행 리스트 (`1/2명 (모집중)`, `2/2명 (마감)`)

### 2.5 1:1 채팅 및 약속 대시보드
- **1:1 채팅방 (`ChatView`)**: 매칭된 상대와의 실시간 메시지 교환, 상단 상대방 프로필(응답률 98%, **당도 99**), 상단 약속 미니 바
- **약속 대시보드 (`DashboardModal`)**: 약속 일시, 상세 주소지(르브런치 대치본점), 파트너 프로필 및 동행 팁, 채팅방 바로가기

### 2.6 마이페이지 (MyPage)
- 사용자 기본 정보: 프로필 사진, 닉네임(`다정한이웃`), 인증회원 뱃지
- **당도 게이지**: **`당도 99 🍯`** (기본 당도 50에서 49 올랐어요!)
- 동행 통계: 참여한 동행(14회), 동행 평점(4.9), 받은 후기(8개)
- 진행 예정 동행 카드 및 취향 키워드/동네 인증 메뉴

---

## 3. 데이터 및 인터페이스 규격 (Types)

```typescript
export interface MeetupPost {
  id: string;
  category: string;
  title: string;
  author: string;
  avatar: string;
  time: string;
  location: string;
  currentMembers: number; // 1 (모집중) 또는 2 (매칭완료)
  maxMembers: number;     // 무조건 2로 고정
  tags: string[];
  status: 'recruiting' | 'closed';
  imageUrl?: string;
}

export interface Appointment {
  id: string;
  status: string;
  dDay: string;
  appointmentBadge: string;
  title: string;
  dateTime: string;
  location: string;
  addressDetail: string;
  partnerName: string;
  partnerAvatar: string;
  partnerRating: number;
  partnerBio: string;
  menuRecommendation: string;
  confirmedGuests: number; // 2명
  totalGuests: number;     // 2명 (1:1 매칭)
}
```

---

## 4. 전체 구현 로드맵

상세한 전체 기능 단계별 구현 계획(Phase 1 ~ Phase 6)과 우선순위(P0~P2), 마일스톤 및 완료 기준은 [ROADMAP.md](file:///Users/lesser/30project/yumidang5/yumidang5/ROADMAP.md) 문서를 참고하십시오.

1. **Phase 1: 사용자 인증 및 신원 신뢰 (AUTH & KYC)** - 휴대폰 OTP, 실명 마스킹, 선택형 KYC
2. **Phase 2: 1:1 동행 공고 탐색 & 관리 고도화 (POST)** - 상세 비밀장소 마스킹, 공고 관리
3. **Phase 3: 1:1 참여 요청, 매칭 확정 및 조율 (MATCH & CHAT)** - 단일 확정 락, 조건 변경 제안 카드
4. **Phase 4: 만남 진행 및 안심 안전 장치 (SAFETY & CALL)** - 안전수칙, 노쇼/신고, 안심 음성통화
5. **Phase 5: 동행 완료, 상호 블라인드 평가 및 당도 정산 (REVIEW & SUGAR)** - 당도 실시간 정산
6. **Phase 6: 유료 전문 동행 및 오퍼 관리 (PAID OFFER)** - 전문 동행 확장

