# Claude 단계별 구현 기록

각 단계에서 실제 변경·검증·보류를 덧붙인다.

## 단계 1 — 공통 상태·데모 기반 (2026-09-15, claude-opus-5)

### 구현
- **공통 상태 정리**: App 안의 예시 시드(공고·신청·대화방·약속·알림·후기)를 `src/data/prototypeSeed.ts`의 `createSeedData()`로 옮겨 초기 로드와 초기화가 같은 원본을 쓴다. App은 저장본 또는 시드 하나에서 모든 상태를 시작한다.
- **새 타입** (`src/types.ts`): `FavoriteFriend`, `Invitation`(invitationId/postId/senderId/recipientId/receivedAt), `CompletionConfirmation`(개인별), `AppointmentReview`(appointmentId+reviewerId), `NotificationSettings`(처음 보는 사람 초대 알림, 기본 ON), `BlockRelation`, `DemoSettings`(시간 오프셋·02/04/07 A/B). `NotificationItem`에 recipientId/createdAt/targetType/targetId와 초대·새 공고·완료·평가 종류 추가. `CurrentUser`에 birthDate/hobbies/traits/isSample 추가. App 공통 상태로 보유·저장한다.
- **버전 저장소** (`src/utils/prototypeStore.ts`): `yumidang:demo:v1`(체험)과 `yumidang:app:v1`(일반)을 분리. 봉투 `{version, savedAt, data}` + 구조 검증. 손상·다른 버전은 화면을 예시로 살리고 경고를 띄우며, 사용자가 `저장 다시 시도`/`저장 데이터 초기화`를 누르기 전에는 깨진 원본을 덮어쓰지 않는다. 용량 초과는 이전 저장본을 유지하고 안내한다. 300KB 넘는 data: 이미지(사진 원본)는 저장하지 않고 표식으로 바꾼다.
- **명시적 데모** (`src/utils/demoMode.ts`, `src/components/DemoControlPanel.tsx`): `?demo=1`일 때만 상단 노란 `체험 설정` 영역이 보인다(서비스 버튼과 구분). 내용: 현재 역할·한국 시간 표시, 예시 계정 5개와 로그아웃 상태로 역할 전환, +1시간/+1일/+7일/시각 지정/실제 시각 복귀, 02·04·07 A/B 선택, 확인 후 데모 키만 초기화(일반 저장 데이터는 그대로). 역할 전환은 같은 브라우저 저장소 안에서 userId만 바꾸며, 기기 간 동기화가 아니라고 화면에 적었다. 체험 모드는 로그아웃 상태로 시작하므로 기존 헤더 `로그인` 흐름을 그대로 쓸 수 있다.
- **새로고침 보존**: 로그인 역할·현재 탭·공고·신청·대화(초안 포함)·약속·알림·관심친구·초대·완료·평가·알림설정·시간 오프셋·A/B를 저장한다.
- **시간 전환 반영**: `clock()`을 App의 모집 마감·수정·신청·작성자 수락(`acceptRequest`)·대화 접근(`roomAccess`)·재동의·취소·완료/평가 판단에 넘긴다(`conversations.ts`에 선택 인자 `now` 추가, 기본값은 기존 동작).
- **외부 요청 격리**: 데모에서는 Supabase 세션 복원·인증 리스너·로그아웃 호출을 하지 않고, 클라이언트도 세션 저장/자동 갱신을 끈다. `trackFunnelEvent`/`trackBackEvent`는 데모에서 바로 반환한다. 기존 연동 코드는 일반 모드에서 그대로다.
- **당도15·공개정보 공통화** (`src/data/publicProfiles.ts`, `src/data/demoAccounts.ts`): `NEW_USER_SUGAR = 15`. 새 가입자는 고유 `user-<uuid>`, 당도 15, 휴대폰 인증 배지 없음(인증번호 확인이 체험이라서). Supabase 세션 사용자도 15. 로그인은 예시 회원(조*미) 계정 레코드를 그대로 써서 모든 화면의 공개정보가 같다. `publicProfileOf(member, directory)` 하나로 공고·신청·대화·약속 모두에서 같은 사진/소개/취미/당도를 만들고, 전화·실명·생년월일·이메일·추천코드는 넣지 않는다. Me는 `인증 전` 표시와 `합산·하한·갱신 시점은 논의 중` 안내로 바꿨고, 기본 50 기준 증감 문구는 없앴다.
- **권한·관계 함수**: `src/utils/access.ts`(비공개 영역 로그인 요구, 확정·완료 당사자만 상세장소, 수신자 지정 알림 필터 — App 알림 목록·안 읽은 수에 적용), `src/utils/relations.ts`(수신자 기준 저장 여부 AND 실제 동행 여부로 '처음 보는 사람' 판단, 알림설정 기본값).
- **회귀 수정**: 데모 바가 생기면서 채팅 화면 고정 높이(`100dvh-126px`) 때문에 입력창이 하단바 아래로 밀려 `메시지 보내기`를 누를 수 없었다. 패널이 자기 높이를 `--demo-bar-h`로 알리고 ChatView가 그만큼 뺀다.

### 검사 (이 단계에서 직접 실행)
- `npm run lint` PASS
- `npm test` PASS 45/45 (기존 29 유지 + 신규 16: `tests/prototypeStore.test.ts` 저장/로드 왕복·빈 저장소·손상 JSON·잘못된 구조·없는 역할·잘못된 A/B·다른 버전·읽기/쓰기 불가·용량 초과 시 이전본 유지·대용량 사진 차단·데모 초기화가 일반 키 보존 / `tests/demoFoundation.test.ts` demo=1 엄격 판정·한국시간 변환·신규 당도15/배지 없음/진입점 무관 동일 프로필·개인정보 비노출·샘플 표시·상세장소 권한·알림 수신자·처음 보는 사람 AND·단방향 저장·시간 전환 시 신청자 수락 불가/마감 후 수락 불가)
- `npm run build` PASS (기존 번들 크기 경고만)
- `node tests/browser/demo-foundation.cjs` (개발서버 `http://127.0.0.1:4186/?demo=1`, 390×844·1280×900) PASS 5/5, 페이지 오류 0, Supabase/분석 요청 0 → `docs/overnight/evidence/step1-demo-foundation.json`, `step1-demo-panel-mobile.png`
  - S1 일반 주소에는 패널 없음, 데모는 로그아웃 상태·헤더 로그인으로 시작
  - S2 로그인→채팅→새로고침 유지, 서*진 전환·+1일·B안이 새로고침 후 유지, 서*진에게 조*미·김*수 대화방 비노출
  - S3 손상 저장본: 경고와 앱 화면 유지, 원본 미덮어씀, 다시 시도 후에도 경고, 초기화 후 정상 v1 저장
  - S4 초기화: 시간 오프셋 0, 예시 역할 유지, 일반 키 그대로
  - S5 로그인·공고 상세·로그아웃 중 외부 요청 0
- `node tests/browser/conversations.cjs` (기존 신청→대화→작성자 수락 회귀, `?demo=1`) PASS 6/6 → `docs/overnight/evidence/production.json`. 첫 실행에서 위 채팅 입력 가림 회귀 3건이 FAIL → 수정 후 전부 PASS.
- `tests/browser/post-lifecycle.cjs` **NOT_RUN**: 출력 경로가 기존 사용자 증거 폴더(`docs/post-lifecycle/evidence`)로 고정돼 있어, 보존 원칙상 실행하지 않았다. 단계 6 통합 검사에서 경로를 받게 고치거나 같은 기준을 overnight 검사에 포함해야 한다.

### 남은 문제·다음 단계로 넘김
- 기존 알림 생성 지점은 아직 recipientId를 넣지 않아 역할을 바꿔도 모두에게 보인다(필터는 적용됨). 단계 4에서 수신자를 지정한다.
- ChatListView·CreateMeetupModal 등 컴포넌트 내부 판단은 아직 실제 시각을 쓴다. 해당 단계에서 체험 시각을 넘긴다.
- A/B 값은 저장·전환만 되고, 화면 반영은 단계 2(프로필)·3(공고)·5(평가)에서 한다.
- 관심친구·초대·완료확인·평가·차단은 타입·저장·관계 판단까지만 준비했다. 화면 흐름은 단계 4·5.
- 기존 가짜 당도 정산(`handleSettleSugar`)과 KYC 체험 배지는 단계 5·2에서 정리한다.

## 단계 2 — 가입·내 프로필·상대 프로필 (2026-09-15, claude-opus-5)

이전 호출이 사용량 제한으로 끊기기 전에 남긴 부분 구현(아래 파일)을 먼저 검사했다. 타입·단위는 통과했지만 브라우저 검사가 5건 중 4건 FAIL이어서 원인을 고치고 검사를 보강했다.

### 구현 (부분 구현 확인 + 이번 수정)
- **검증 규칙 공통화** (`src/utils/profile.ts`): 전화 숫자만 11자리(입력·붙여넣기 동일, 01로 시작), 한글 실명(자모 낱자/영문·공백·숫자 구분 안내, 2~10자, 저장/다음에서만 검사해 IME 조합 방해 없음), 생년월일(실제 달력 날짜·한국 날짜 기준 미래 차단·만 19세), 공개용 연령대 계산, 남성 추천인 코드/학교·직장 이메일 형식 검사(개인 메일 거부), 사진 JPG/JPEG/PNG·10MB 이하(정확히 10MB 허용), 소개 필수·300자·연락처/메신저/금칙어 안내, 프로필 미완성 단계 계산(저장 차단된 사진 표식은 사진 없음으로 처리), 사진 없는 사용자용 중립 placeholder.
- **01 A 가입** (`src/components/AuthModal.tsx`): 헤더 `로그인` → 팝업 위 탭 없음 → 하단 `휴대폰 본인인증으로 가입` → 약관 → 번호(실제 문자 미발송 안내·예시 코드) → 기본정보(실명·생년월일·성별·지역, 남성은 추천인 코드 또는 학교·직장 이메일 예시 확인) → `로그인으로 돌아가기`. 신규 가입자는 당도 15, 휴대폰/KYC 배지 없음, 생년월일 저장·공개는 연령대만.
- **프로필 만들기/편집** (`src/components/ProfileEditor.tsx`, `src/utils/imageResize.ts`): 첫 가입/미완성 로그인 뒤 사진 → 취미·성향 → 소개 → 확인/저장 4단계. 사진은 선택 → 미리보기(미저장 표시) → `이 사진으로 저장` → 변경/삭제(삭제 확인·재등록 안내). 원본은 저장하지 않고 480px JPEG로 줄여 보관, 손상 파일은 다시 선택 안내. Me `프로필 편집`은 한 번에 저장, 저장하지 않은 변경은 닫을 때 확인. 설정 중·편집 중 모두 `공개 프로필 미리보기`.
- **미완성 복귀** (`src/App.tsx`, `MyPageView.tsx`): 신청·공고 작성 시 첫 미완성 단계로 이동. 이번 수정으로 안내 문구를 전역 알림 대신 편집기 안 경고로 띄운다(전역 알림이 편집기 `다음` 버튼을 가려 누를 수 없던 문제). 상단 미완성 바와 Me 안내에서 `이어서 작성`, 새로고침 뒤에도 남은 단계부터 재개.
- **당도·배지** (`MyPageView.tsx`, `UserProfileModal.tsx`, `KycAuthModal.tsx`): 신규 `15 🍯`·`인증 전`, 샘플 회원은 `샘플 수치`/`예시` 표시, 합산·하한·갱신 논의 중 안내. KYC 창은 흐름 미리보기만 하고 배지를 주지 않으며, 이번에 dialog 역할·닫기 이름·Esc를 추가했다.
- **02 A/B 상대 프로필** (`UserProfileModal.tsx`): A는 사진·이름·지역·연령대·취미·소개(3줄)·당도/후기 요약 후 `상세 펼치기`(aria-expanded), B는 같은 정보를 연속 스크롤. 체험 설정의 02 선택으로 전환(사용자에게 매번 묻지 않음). 관심친구 저장/해제(상대 알림 없음), 신고·차단 진입(`SafetyEntryDialogs.tsx`, 예시 접수·영향 안내). 전화·실명·생년월일·이메일·추천코드는 공개 객체에 복사하지 않는다.
- **진입점**: 공고 상세 작성자 요약, 채팅 헤더, Me 받은/보낸 신청, 약속 대시보드 상대 → 모두 같은 userId로 `publicProfileForMember`. 초대 진입점은 초대 화면이 생기는 단계 4에서 같은 함수로 연결한다.
- **이번 수정**
  - 저장 스냅샷이 로그인 사용자의 최신 레코드를 users 목록에 합쳐 저장한다. 이전에는 목록 동기화가 한 렌더 늦어 사진 삭제 직후 삭제 전 사진이 저장돼 있었다.
  - 상세 프로필을 닫으면 원래 누른 작성자 요약으로 포커스가 돌아간다. 아래 공고 창이 inert가 되기 전 첫 렌더에서 포커스 대상을 기억한다.
  - 브라우저 검사의 취미/성향 칩 선택을 편집기 범위로 한정했다(뒤 홈의 `전시` 카테고리 버튼과 이름이 같음). 기대조건은 줄이지 않았다.

### 검사 (이 단계에서 직접 실행)
- `npm run lint` PASS
- `npm test` PASS 54/54 (단계1 45 + `tests/profile.test.ts` 9: 전화 11자리·이름 자모/영문/길이·생년월일 달력/미래/만19 한국날짜 경계·남성 경로·사진 형식/10MB 경계·소개 300/연락처/금칙어·미완성 단계·진입점 동일 공개정보와 개인정보 비노출·축소 사진 저장/원본 차단)
- `npm run build` PASS (기존 번들 크기 경고만)
- `node tests/browser/profile-flow.cjs` (http://127.0.0.1:4186/?demo=1, 390×844 / P4 1280×900) PASS 6/6, 페이지 오류 0, 외부 요청 0 → `docs/overnight/evidence/step2-profile.json`, `step2-signup-basic-mobile.png`, `step2-photo-preview-mobile.png`, `step2-me-new-member-mobile.png`, `step2-profile-A-mobile.png`, `step2-profile-B-mobile.png`, `step2-profile-A-desktop.png`
  - P1 가입·프로필 전체: 13자리 입력/붙여넣기→11, 없는 번호 로그인 거절, 잘못된 인증번호, 빈/자모/영문 이름, 미래/만19 미만, 성별 누락, 추천코드 누락, 개인 메일, 틀린 이메일 코드, gif/10MB+1/손상 사진, 사진 없이 다음, 미저장 미리보기, 저장→삭제(저장본 확인)→재등록, 취미/성향 필수, 소개 301자·전화번호, 공개 미리보기 일치·개인정보 비노출, Me 당도 15·인증 전, 새로고침 후 유지
  - P2 미완성 복귀: 신청 시 사진 단계로 이동(신청창 미개방) → 사진만 저장 후 나감 → 새로고침 뒤 바/Me에서 취미·성향부터 재개
  - P3 공고→작성자→A 펼치기/B 연속, 뒤로·Esc로 공고 복귀+포커스, A/B 간 관심친구 유지, 새로고침 후 저장 1건·대상 알림 없음
  - P4 채팅·받은 신청·공고 작성자 진입점이 같은 당도/기본정보
  - P5 호스트가 본 신규 신청자: 당도 15, 인증 없음, 같은 사진·소개, 연령대만, 예시 라벨 없음
  - P6 Me 편집: 소개 301자·메신저 아이디 거절, 초안 미리보기, 저장 후 새로고침 유지, 미저장 변경 닫기 확인·버리기, 상대(서*진)가 채팅에서 같은 소개 확인
- 회귀: `node tests/browser/demo-foundation.cjs` PASS 5/5, `node tests/browser/conversations.cjs` PASS 6/6 (첫 실행은 브라우저 3개 동시 실행 중 페이지 로드 시간 초과였고, 단독 재실행에서 PASS)
- `tests/browser/post-lifecycle.cjs` NOT_RUN (단계 1과 같은 이유: 기존 사용자 증거 폴더에 덮어씀)

### 남은 문제·다음 단계로 넘김
- 초대 카드의 상대 프로필 진입은 초대 화면과 함께 단계 4에서 연결.
- 신고는 진입·입력검증·예시 접수 안내까지. 신고 이력·설정 목록·차단 해제는 단계 5.
- 기존 가짜 당도 정산(`handleSettleSugar`)과 리뷰 모달의 점수 연출은 단계 5에서 제거.
- 사진은 브라우저 저장 공간에 축소본만 저장. 실제 서버 업로드·이미지 검수는 하지 않는다.
- 소개 연락처/금칙어는 프론트 규칙 검사이며 실제 서버 필터가 아니다.

## 단계 3 — 탐색·공고 작성 A/B·Me (2026-09-15, claude-opus-5)

시작 시 src/tests에 단계 2 이후 변경이 없어(부분 구현 없음) 단계 2 코드 위에서 새로 구현했다.

### 구현
- **탐색 필터** (`src/utils/explore.ts`, `ExploreView.tsx`): 목록 바로 위에 검색·카테고리(지금이 첫 칸 … 기타)·지역(공개 지역+랜드마크 키워드)·날짜(전체/오늘/7일 이내/날짜 지정, 동행 시작일 기준, 한국 날짜). 적용 요약·결과 건수·`필터 초기화`, 0건 화면(초기화/홈으로). 필터 상태는 App이 보관해 공고 상세를 닫거나 탭을 오가도 유지. 카드 작성자 당도는 가짜 `99` 대신 공개 프로필 값(카테고리 목록 화면도 같이 수정).
- **이벤트 → 관련 공고 → 같은 행사로 작성** (`EventDetailModal.tsx`, `data/events.ts` `eventById`, `prototypeSeed.ts`): 이벤트 상세는 그 eventId로 작성된 공고만 보여주고(일반 공고 미혼합) `이 행사로 동행 모집하기` → 작성 폼에 연결 행사 표시 → 저장 공고에 같은 eventId. 종료 행사는 버튼 비활성+이유(App에서도 차단), 상세는 계속 읽기 가능. 이번 주차 시작 행사 하나에 연결된 예시 공고 2건을 시드에 추가(1건은 여성만 조건). 공고 상세에 `연결 행사` 표시.
- **공고 작성 04 A/B** (`src/utils/postForm.ts`, `CreateMeetupModal.tsx`): 필드 모델·검증·저장 변환·편집 복원을 한 모듈로 공유. A 한 장, B 두 단계(1 기본 정보·일정 / 2 장소·상대 조건, 요약·이전). 검증: 빈 필수값(제목·소개·공개 지역·랜드마크), 지난 시작, 종료>시작, 현재<모집 마감<=시작. 필드별 오류 표시와 첫 오류로 포커스, B는 1단계 오류면 1단계로 복귀. 기존 제목·카테고리·소개·날짜·시작/종료·모집 마감·공개 동네·상세 장소·태그·바라는 점 보존 + **상대 성별 조건**(무관/여성/남성) 추가. 체험 설정 04 선택으로 전환(데모에서만 `04 A안/B안` 표시). 유료 PRO 옵션은 별도 체험으로 유지. 등록 즉시 전체 공개, 작성자 본인에게만 등록 알림(수신자 지정).
- **상대 조건과 신청** (`requestEligibility`, `PostDetailModal.tsx`, App): 조건 충족자는 신청, 미충족자는 상세에서 비활성 버튼+이유, 신청 함수에서도 차단. 비회원은 조건만 보임.
- **재동의** (`postLifecycle.ts`): `conditionsOf`/`conditionChanges`에 `상대 성별 조건` 추가 → 기존 신청은 `변경 조건 확인 필요`, 동의해도 `매칭 중 · 확정 전`, 작성자 수락이 있어야 확정. 변경된 조건에 맞지 않는 신청자는 동의가 막히고, 작성자 수락도 이유와 함께 막힘. 모집 마감 시각만 바꾸는 편집은 재동의 대상이 아님(기존 만료 회귀 기대 유지).
- **Me** (`MyPageView.tsx`, `CompanionRequests.tsx`, `src/utils/myActivity.ts`): 상단 신청한 동행/받은 신청 두 탭 유지. 보낸 신청 카드에도 작성자(상대) 프로필 진입, 종료된 신청은 상태 라벨+이유 문장(거절/취소/다른 신청자 확정/마감/만료/삭제/조건 거절/확정 취소), 수락 불가 이유, `대화 없이 바로 수락 가능` 안내. **초대 관리** 골격(받은/보낸 초대 탭·카드·빈 상태, 초대만으로 신청·확정 안 됨 안내; 초대 생성은 단계 4). **내가 쓴 공고**(상태 라벨, 확정 전 신청 수, 마감/만료/삭제/매칭/취소 이유 — 모집 마감은 완료가 아님을 명시). **상태별 동행** 확정/완료/취소 탭: 약속 제목(약속 상세), 연결 공고, 보는 사람 기준 상대(방 멤버·계정으로 계산, 저장된 partnerName의 한쪽 시점 오류 회피), 취소 주체·사유, 약속 상세/대화방. 기존 중복 목록 `나의 동행 (n건)`은 이 탭으로 대체.
- **이번 호출에서 찾은 버그 수정**: B안 1단계 `다음` 클릭 시 React가 같은 `<button>` 노드를 `type="submit"`으로 재사용해 기본 동작으로 폼이 제출되던 문제(2단계 입력 없이 기본값으로 공고가 저장됨). 버튼별 key로 분리.
- **검사 실행 보조** (`tests/browser/overnight-env.cjs`): 환경변수가 없을 때 기존 `conversations.cjs`가 사용자 증거 폴더(`docs/prototype-roadmap/evidence`)에 쓰지 않도록 overnight 기본값(CHECK_URL 4186 데모, EVIDENCE_DIR docs/overnight/evidence)을 채워 실행.

### 검사 (이 단계에서 직접 실행)
- `npm run lint` PASS
- `npm test` PASS 61/61 (단계2 54 + `tests/explorePostMe.test.ts` 7: 폼 필수값·지난 시작(현재와 같은 시각 포함)·종료<=시작·다음날 종료·마감=시작 허용/시작+1분 거절/마감=현재 거절; B 단계 필드가 A 전체와 같음·A/B 저장 필드 동일·편집 복원 왕복; 상대 조건 충족/미충족/비공개 성별/비회원; 성별 조건 변경→재동의→동의만으로 미확정→신청자 수락 불가→작성자 수락 시 1명 확정·나머지 종료, 마감시각만 변경은 재동의 없음; 필터 카테고리·지역(랜드마크 포함)·오늘·7일 경계(6일 포함/7일 제외)·날짜 지정·검색·0건·요약 라벨·월 넘김; Me 목록이 수락/취소/마감/삭제 공통 상태와 양쪽에서 일치, 작성자만 삭제; eventById 월 무관)
- `npm run build` PASS (기존 번들 크기 경고만)
- `node tests/browser/explore-post-me.cjs` (http://127.0.0.1:4186/?demo=1) PASS 8/8, 페이지 오류 0, 외부 요청 0 → `docs/overnight/evidence/step3-explore-post-me.json`, `step3-explore-filter-mobile.png`, `step3-explore-empty-mobile.png`, `step3-explore-desktop.png`, `step3-event-related-mobile.png`, `step3-post-form-A-mobile.png`, `step3-post-form-B-mobile.png`, `step3-me-status-mobile.png`, `step3-me-desktop.png`, `step3-explore-after-delete-mobile.png`
  - E1 전시→종로구→7일→날짜 지정 0건→초기화, 오늘, 저장 데이터로 계산한 기대 건수와 카드 수 일치, 상세 닫기·탭 이동 뒤 필터 유지, 0건 화면 홈 복귀 / E1D 1280×900
  - E2 이벤트 상세 관련 공고 2건 모두 같은 eventId → 같은 행사로 작성 → 3건·저장 eventId 일치, 다른 행사에는 미표시, 이전 달 종료 행사는 작성 버튼 비활성+이유
  - E3 A: 빈 제목/소개, 지난 시작, 종료<시작, 마감>시작 오류 후 저장 → 편집 시 값 복원 / B: 1단계 오류 유지, 2단계 필수값, 같은 값 저장 → A와 저장 필드 동일, B 편집 복원·무변경 저장 / 타인(서*진) 수정·삭제 버튼 없음, 남성만 조건에서 여성은 이유와 함께 차단, 남성(김*수)은 신청 가능
  - E4 성별 조건 변경 → 받은 신청 `조건 동의 대기` 비활성 → 신청자 동의 → 확정 없음·신청자 수락 버튼 없음 → 작성자 수락 → 같은 방에 약속 연결, 다른 후보 `다른 동행자와 확정`+이유, 양쪽 Me 확정 탭에 상대 이름 정확
  - E5 김*수 신청 → 확정 전 대화 → 탭 이동·새로고침 뒤 대화 유지 → 서*진 수락(1건만 accepted, 조*미 신청 종료) → Me 확정·홈 리마인드 → 확정 동행 취소 → 확정 탭에서 빠지고 취소 탭에 `내가 취소했어요 · 사유`, 홈 리마인드 제거, 차단 관계 없음, 상대 Me에는 `상대가 취소했어요`, 탐색 카드 `동행 취소 · 모집 종료`
  - E6 모집 마감 → 내가 쓴 공고 이유(완료 아님)·완료 0 유지, 신청자 이유 → 삭제 → 삭제 이유, 옛 링크 `삭제된 공고`에서 이전 화면으로, 탐색 목록에서 제거
  - E7 1280×900 Me 확정 탭 기본·작성 공고 수·초대 관리 빈 상태·탭 전환
  - 첫 두 실행에서 E3 FAIL → 원인이 위 B안 `다음` 제출 버그였고 수정 후 PASS
- 회귀: `node tests/browser/overnight-env.cjs tests/browser/conversations.cjs` PASS 6/6 → `docs/overnight/evidence/production.json`. 첫 실행은 2건 FAIL(Me에서 `강*훈 님과의 공원 산책` 약속 버튼이 사라짐) → 약속 제목 버튼을 상태별 동행 기본 탭(확정)에 두고 `내가 쓴 공고`를 항상 보이게 바꾼 뒤 PASS.
- 회귀: `node tests/browser/demo-foundation.cjs` PASS 5/5, `node tests/browser/profile-flow.cjs` PASS 6/6 (연속 실행 중 한 번 P5가 페이지 로드 networkidle 시간 초과 — 기대조건 실패 아님, 단독 재실행 PASS)
- `tests/browser/post-lifecycle.cjs` NOT_RUN (단계 1·2와 같은 이유: 기존 사용자 증거 폴더에 기록). 이 파일이 쓰는 선택자(`동행 공고 작성/수정` 대화상자, `시작 날짜`·`모집 마감 날짜·시각`(max 속성 유지), `내가 쓴 공고` 영역, `공고 수정`)는 유지했다.

### 남은 문제·다음 단계로 넘김
- 초대 생성·수신·알림·정렬·관계 표시는 단계 4(이번엔 Me 초대 관리 골격과 invitationId/postId/senderId/recipientId 목록만).
- 수락·거절·마감·취소 등 기존 알림 생성 지점의 수신자 지정은 단계 4(신규 공고 등록 알림만 작성자 지정).
- 계정 레코드가 없는 예시 신청자(이*은)는 성별을 알 수 없어 상대 조건 판정을 하지 않는다(시드 한정).
- 확정 약속의 일정 변경은 기존 채팅 변경 제안(상대 동의 후 적용)을 그대로 쓴다. 일정 겹침은 경고 후 진행 유지, 새 제한/페널티 없음.
- 시간 전환 시 ChatListView 등 일부 컴포넌트 내부 판단의 실제 시각 사용은 단계 6 통합 검토.


## GPT 인수인계 확인 — 2026-09-15T15:30:19+09:00

- 최신 사용자 요청으로 구현 담당을 GPT/Codex로 전환. Claude 실행기52794 종료 및 재실행 차단을 실제 확인했다. 이전 로그는 보존했다.
- 단계1~3 PASS 기록, 단계4 한도 응답과 결과 없음, 단계5~6 미시작을 확인했다. 이 기록은 새 기능 구현 완료가 아니다.
- 현재 타입·단위72개·별도 출력 빌드 PASS. 실제 Chromium 모바일/데스크톱 루트 주소 기본 이동 2개 PASS, 오류/외부 요청 기록0. 전체 B01~B15는 미실행.
- 기존 중간 미리보기의 하위 주소에서 Me 새로고침이 실패하는 경로 문제를 발견했다. 이전 정상 사본은 유지하고 현재 루트 주소 검사를 별도로 실행했다.
- 다른 세션의 App/Auth/Supabase·환경 변경은 보존했다. DB 읽기/쓰기·마이그레이션·환경 변경·GitHub/Vercel 게시를 하지 않았다.
- 남은 단계, 공유 파일 경계, 상세 증거와 다음 작업은 [GPT_HANDOFF.md](GPT_HANDOFF.md)에 기록했다.
